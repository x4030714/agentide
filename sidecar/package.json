{
  "name": "agentide-sidecar",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Agent host: runs @anthropic-ai/claude-agent-sdk and speaks JSON lines to the Rust core.",
  "main": "dist/main.mjs",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types src/*.test.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.261",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "typescript": "~5.8.3"
  }
}
